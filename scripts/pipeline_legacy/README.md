# Legacy Pipeline Import Area

Place the original Google Drive scripts used in Update 1/2 here before refactoring into `packages/ml`.

Recommended subfolders:
- `extract/`
- `preprocess/`
- `labeling/`
- `analysis/`

Keep original filenames intact for traceability, then progressively migrate stable logic into reusable package modules.
