# ESG Chatbot API

FastAPI backend serving:
- Company profile APIs
- Dashboard and comparison APIs
- Retrieval-grounded chat APIs

Run:

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```
